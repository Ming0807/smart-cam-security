# Database Folder

This folder will contain SQL migration files for Supabase.

Recommended first migration:

```text
database/migrations/001_initial_schema.sql
```

The initial schema is documented in:

```text
docs/DATABASE_SCHEMA.md
```
