---
description: Run or describe tests for the active task
agent: build
---

Test the current active task.

If tests can be run locally, run them.

If hardware is required, create a manual hardware test checklist.

Update docs/TEST_PLAN.md if new tests are added.
